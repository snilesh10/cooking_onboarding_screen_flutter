import 'package:flutter/material.dart';

const titleStyle = TextStyle(fontSize: 25.0, fontWeight: FontWeight.w700);

const descStyle = TextStyle(fontSize: 18.0, fontWeight: FontWeight.w300);

const whiteBoxStyle = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(topLeft: Radius.circular(80.0)));
